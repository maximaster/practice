#import "../utils/page.typ": page
#import "../utils/glossary.typ" as g
#import "../utils/repo.typ": logbook-cap

#show: page.with(title: "Задание", active: "/task/")

В репозитории хранится начатый проект «Recall» — персональная база знаний с
#g.spaced-repetition()[интервальным повторением]. Backend на PHP, frontend на TypeScript, схема API
описана в `spec/api/openapi.yaml`. Проект запускается, и даже что-то уже работает.

= Что уже есть

- #g.note()[Заметки]: создание, чтение, изменение, удаление, фильтрация по тегу.
- #g.card()[Карточки]: создаются из заметок.
- Повторение: #g.review-queue()[очередь на сегодня] и выставление оценки карточке.
- Статистика: сколько повторить сегодня, сколько за неделю, #g.streak()[серия дней].

= Сущности

- Заметка — заголовок, текст, теги, ссылки на другие заметки.
- Карточка — лицевая и оборотная сторона, создаётся из заметки.
- Повторение — #g.grade() (`again` / `hard` / `good` / `easy`) и дата
  следующего показа.

= Про алгоритм повторения

Используется упрощённый интервальный алгоритм в духе #g.sm2():
чем лучше вы помните карточку, тем дальше отодвигается следующий показ; чем хуже
— тем ближе. Конкретные интервалы и правила подсчёта статистики намеренно не
зафиксированы в задании — это часть того, что вам предстоит определить и
реализовать. #g.acceptance-tests()[Приёмочные тесты] проверяют поведение, а не конкретные числа.

= Как определить готовность

`just verify` запускает приёмочные тесты. Они нарочно нестрогие.

Команда поднимает приложение, гоняет HTTP-проверки (#g.hurl()[Hurl]) по схеме из
`spec/api/` и несколько #g.playwright()[Playwright]-сценариев.

Поднять руками для разработки:

```sh
just dev       # запустить приложение в режиме разработки через docker compose
just test      # запуск backend юнит-тестов
just lint      # провести статанализ и проверку стиля кода (входит в verify)
just fmt       # автоформат и безопасные автоправки
just guide     # собрать сайт-инструкцию локально
```

= Структура репозитория

- `www/` — данная инструкция (Typst + Tola)
- `app/backend/` — PHP 8.5, Slim 4, Cycle ORM, SQLite
- `app/frontend/` — фронтенд на TypeScript + Vite
- `spec/api/` — схема OpenAPI
- `spec/acceptance/` — приёмочные проверки (`*.hurl`)
- `e2e/` — Playwright-сценарии
- `compose.yaml` — конфигурация docker compose для запуска приложения
- `Justfile` — команды `just verify`, `dev`, `guide`, `test` и др.
- `LOGBOOK.md` — журнал решений (#raw(logbook-cap) строк)

= Как сдавать работу

1. Форкните публичный шаблон репозитория.
2. Работайте в отдельной ветке, а не в `master`: автопроверка `just verify`
   в CI гоняется на ветках и в PR.
3. Доведите проект до успешного прохождения `just verify`.
4. В процессе заполняйте `LOGBOOK.md`.
5. Откройте Pull Request (PR) из своей ветки и убедитесь, что проверки прошли в CI.
6. Пришлите нам ссылку на PR.

Удачи!
